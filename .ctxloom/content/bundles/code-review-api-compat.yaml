version: 1.0.0
tags:
- review
- api
- compatibility
- library
author: ctxloom
description: API design & backward-compatibility review. Language-agnostic — a single `general` fragment. Compose `code-review-api-compat#fragments/general` as a `ctxloom weave` member.
fragments:
    general:
        tags:
        - review
        - api
        - compatibility
        - library
        content: |-
            # Lens: API Design & Backward Compatibility

            For public/library/service interfaces and wire/schemas: is the design sound,
            and does this change break existing consumers?
        content_hash: sha256:d42c285bb27a11ac307499079229a49102f27886eed3d885ce08ee2ab70d1e0c
        distilled: |-
            # Lens: API Design & Backward Compatibility
            For public/library/service interfaces and wire/schemas: is the design sound, and does this change break existing consumers?
        distilled_by: claude-code
