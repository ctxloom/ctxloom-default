---
distilled_by: claude-code
---
React: one folder per component (`Button/Button.tsx` + `Button.test.tsx` co-located + `index.ts` re-export). Function components only; named exports; explicit `React.ReactElement` return type; props interface declared above the component.