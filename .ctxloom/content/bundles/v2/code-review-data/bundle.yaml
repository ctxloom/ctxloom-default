version: 1.0.0
tags:
    - review
    - data
    - migrations
    - privacy
author: ctxloom
description: Data, migrations & privacy review. Language-agnostic — a single `general` fragment. Compose `code-review-data#fragments/general` as a `ctxloom weave` member.
name: code-review-data
