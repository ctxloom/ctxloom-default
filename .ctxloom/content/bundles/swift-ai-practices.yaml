name: swift-ai-practices
version: 1.0.0
tags:
- swift
- ios
- macos
- ai
- best-practices
author: ctxloom
description: Swift/iOS AI coding practices - project files, concurrency, SwiftUI
fragments:
    swift-rules:
        tags:
        - swift
        - ios
        - coding
        content: |-
            # Swift Rules

            ## CRITICAL: Never Modify .pbxproj

            NEVER modify `.xcodeproj/project.pbxproj` files.

            1. Create files with AI
            2. Add to Xcode manually (File > Add Files)
            3. Let Xcode manage the project file

            One corrupted .pbxproj wastes hours.
        content_hash: sha256:4520c0834eecf71643474cc04527e3faa3ebfaeec07dbe674d9ea3b20e85d21a
        distilled: NEVER modify .xcodeproj/project.pbxproj. Create files, add them via Xcode (File > Add Files), let Xcode manage the project file.
        distilled_by: claude-code
