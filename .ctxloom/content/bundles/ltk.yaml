name: ltk
version: 1.0.0
tags:
    - default
    - ltk
    - tooling
    - hooks
author: ctxloom
description: llm-tool-killer (ltk) — how the command-redirect hook steers the agent, and the rails it enforces.
notes: |
    Context for working in a project that uses ltk (llm-tool-killer), the
    ctxloom sibling that guides the commands an agent runs. This bundle ships
    guidance only; install the hook itself with `ltk manage install` (it writes
    the agent hook + .ltk/config.yaml). Pairs with the conduct/code-quality
    baseline in the default profile.
fragments:
    ltk:
        tags:
            - ltk
            - tooling
        content: |
            # llm-tool-killer (ltk)

            This project may run **ltk**, a pre-tool hook that inspects each shell
            command before it executes and redirects it when a rule matches. Where
            ctxloom shapes the context you see, ltk guides the commands you run.

            ## What it does

            ltk parses the real command (resolving variables, unwrapping trivial
            wrappers and sub-shells) and matches it against the project's rules in
            `.ltk/config.yaml`. The first matching `deny` wins and returns a
            `message`/`suggest` telling you what to run instead. Example:

                go test ./...   ->   blocked: "Run tests through the task runner."
                                ->   retry with `just test`

            ## How to work with it

            - Treat a redirect as guidance, not a failure: read the suggestion and
              retry the command the way the rule asks.
            - Prefer the project's task runner (e.g. `just <target>`) over invoking
              build/test/lint tools directly.
            - **Agents do not cut releases.** ltk blocks `git tag` and release
              commands. Prepare the version bump and PR; a human (or CI) cuts the tag.

            ## What it is not

            ltk is a cooperative redirect, not a sandbox. If explicitly instructed to
            work around a rule the agent can, so it makes the easy, accidental path
            the right one rather than enforcing hard isolation. For strict "never"
            boundaries, run the agent in a container.
        content_hash: sha256:097c1e35d3792346b91684ca81f613179c39abe07f361367a982a33317a212bb
        distilled: "# ltk\n\n**llm-tool-killer (ltk)** — pre-tool hook that inspects and redirects shell commands per `.ltk/config.yaml` rules.\n\n## How it works\n\nParses real command (resolving variables, unwrapping wrappers) → matches against project rules → first matching `deny` returns `message`/`suggest`:\n\n    go test ./...   →   blocked: \"Run tests through the task runner.\"\n                    →   retry: `just test`\n\n## How to use\n\n- Treat redirects as guidance; read suggestion and retry as specified\n- Prefer project task runner (`just <target>`) over invoking tools directly  \n- **Agents do not cut releases** — ltk blocks `git tag`/release commands; prepare version bump/PR for human or CI\n\n## What it is not\n\nCooperative redirect, not a sandbox. Explicit workarounds are possible; for strict boundaries use a container."
        distilled_by: claude-haiku-4-5-20251001
commands:
    tooling:
        description: Tools this bundle's content needs where agents run (applied via `ctxloom tooling`, user-approved)
        tags:
            - tooling
        content: |
            This bundle's guidance assumes the ltk hook can actually run, so
            agent environments (the agent container image) need the binary:

            - ltk: Homebrew or a release binary from GitHub
              (https://github.com/ctxloom/ltk). After install, the project
              wires the hook with `ltk manage install` (writes the agent hook
              + .ltk/config.yaml) — don't wire it from the image build.
