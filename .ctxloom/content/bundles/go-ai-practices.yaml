version: 1.0.0
tags:
- golang
- go
- ai
- best-practices
author: ctxloom
description: Go AI coding practices - error handling, concurrency, interfaces
fragments:
    go-rules:
        tags:
        - golang
        - coding
        content: |-
            # Go Rules

            - Assertions: `testify/assert`. Prefer fakes over mocks.
            - No `init()` functions — explicit initialization.
            - No package-level variables — dependency injection.
            - Logging: `slog` with structured attributes.
        content_hash: sha256:68c7d75f6dfcc5be95ce05c76e58cbf7a11a596b741db2db6a13c4595063aabd
        distilled: 'Go: testify/assert; fakes over mocks; no init() (explicit initialization); no package-level vars (DI); slog structured logging.'
        distilled_by: claude-code
