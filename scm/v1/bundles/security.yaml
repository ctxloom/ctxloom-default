version: "1.0.0"
tags:
  - security
  - practices
author: "scm"
description: "Security practices for secrets management, input validation, and secure coding"

fragments:
  security:
    tags:
      - security
      - code
    content: |-
      # Security

      ## Core Principles

      - **NEVER hardcode secrets** — no passwords, API keys, tokens, or credentials in code, config files, or tests. Ever. Use environment variables, secret managers (Vault, AWS Secrets Manager), or encrypted config.
      - Validate all external input
      - Use parameterized queries for databases
      - Keep dependencies updated
      - Follow principle of least privilege

      ## Secrets Management

      - Environment variables for runtime secrets
      - `.env` files for local dev (NEVER commit — must be in `.gitignore`)
      - Secret managers for production (Vault, AWS SM, GCP SM, Azure KV)
      - For tests: use fake/mock values like `test-password`, `FAKE_API_KEY`, or inject from CI secrets
      - If you see a hardcoded secret, remove it immediately and rotate the credential

      ## Security Review Checklist

      - Input validation present and correct?
      - Path traversal vulnerabilities?
      - Injection risks (SQL, command, XSS)?
      - TOCTOU (time-of-check-time-of-use) issues?
      - Privilege escalation paths?
      - Authentication/authorization correct?
      - Secrets properly managed?
      - Cryptography used correctly?
      - Sensitive data exposure?
      - Dependencies scanned for vulnerabilities?
    content_hash: sha256:d7f27ca2fa6365323e677b27b9ac7701a20b196a317103d965cb82646426ea6e
    distilled: |-
      # Security

      ## Core
      - **NEVER hardcode secrets** — use env vars, secret managers (Vault, AWS/GCP/Azure), or encrypted config
      - Validate all external input
      - Parameterized DB queries
      - Keep deps updated
      - Least privilege

      ## Secrets
      - Env vars for runtime
      - `.env` for local (in `.gitignore`, never commit)
      - Secret managers for prod
      - Tests: fake values (`test-password`, `FAKE_API_KEY`) or CI secrets
      - Found hardcoded secret → remove + rotate immediately

      ## Review Checklist
      - Input validation?
      - Path traversal?
      - Injection (SQL, command, XSS)?
      - TOCTOU?
      - Privilege escalation?
      - Auth/authz correct?
      - Secrets managed?
      - Crypto correct?
      - Data exposure?
      - Deps scanned?
    distilled_by: claude-code
