version: "1.0.0"
tags:
  - containers
  - docker
  - podman
  - testing
  - devops
author: "scm"
description: "Container tooling including devcontainers, testcontainers, and overlay patterns"

fragments:
  devcontainer:
    tags:
      - tooling
      - devcontainer
    content: |-
      Dev Containers: Maintain .devcontainer config for consistent dev env w/ required tools, deps, system reqs. Enables reproducible builds.
    content_hash: sha256:eae6e1c99180c400ffbf4cb689c8cd8b766b46f45890af58cebc8eee9296c75c
    distilled_by: claude-code

  container-overlay:
    tags:
      - pattern
      - containers
      - docker
      - podman
      - makefile
      - just
      - devops
    content: |-
      # Container Overlay Pattern

      Same command interface, different execution context. Eliminate conditionals from build files while supporting both host and containerized builds.

      ## The Problem

      Containerized builds for consistency across machines and CI. Common approaches have friction:

      - **Dual Makefiles**: `make -f Makefile.docker build` — people forget which to use
      - **Conditional detection**: `ifeq`/`else`/`endif` blocks everywhere — cluttered, hard to maintain
      - **Different commands**: `make build` vs `make container-build` — parallel names, duplicate docs

      ## The Solution

      Mount a different build file over the host's file inside the container.

      ```
      project/
      ├── Makefile              # Host: delegates to container
      └── Makefile.container    # Container: runs commands directly
      ```

      **Host Makefile** — orchestration:

      ```make
      DOCKER_RUN := docker run --rm \
          -v ./:/workspace \
          -v ./Makefile.container:/workspace/Makefile:ro \
          -w /workspace \
          myimage

      build:
          $(DOCKER_RUN) make build

      test:
          $(DOCKER_RUN) make test
      ```

      **Container Makefile** — execution:

      ```make
      build:
          cargo build

      test:
          cargo test
      ```

      The key: `-v ./Makefile.container:/workspace/Makefile:ro`

      This mounts `Makefile.container` **over** `Makefile` inside the container. The file swap *is* the detection mechanism.

      ## How It Works

      - **On host**: `make build` → runs Docker → mounts overlay → `make build` inside sees `Makefile.container`
      - **In container**: `make build` → runs `cargo build` directly

      No conditionals. No environment checks. No remembering which command.

      ## Separation of Concerns

      **Host file responsibilities:**
      - Container image selection
      - Volume mounts
      - Network configuration
      - Environment variables

      **Container file responsibilities:**
      - Compilation
      - Testing
      - Linting
      - Build logic

      When build logic changes, edit container file. When orchestration changes, edit host file.

      ## With just (Cleaner)

      ```just
      # justfile (host)
      _run +ARGS:
          podman run -v ./justfile.container:/workspace/justfile:ro ... just {{ARGS}}

      build:
          just _run build

      # justfile.container
      build:
          cargo build
      ```

      just advantages over Make:
      - No `.PHONY` declarations
      - Shell variables work naturally
      - Recipes take arguments
      - Better error messages
      - Cross-platform without gnumake/BSD quirks

      ## Edge Cases

      ### Already in a Container (Devcontainer)

      Optional escape hatch:

      ```make
      ifdef DEVCONTAINER
      DOCKER_RUN :=
      else
      DOCKER_RUN := docker run --rm -v ... myimage
      endif

      build:
          $(DOCKER_RUN) make build
      ```

      When `DEVCONTAINER` is set, commands run directly.

      ### Podman

      Same pattern, swap `docker` for `podman`. Add `:Z` for SELinux:

      ```make
      PODMAN_RUN := podman run --rm \
          -v ./:/workspace:Z \
          -v ./Makefile.container:/workspace/Makefile:ro \
          -w /workspace \
          myimage
      ```

      ## Tradeoffs

      **Still duplicated:**
      - Target names in both files
      - Two files to maintain

      **But:**
      - Mechanical duplication, not logical mixing
      - AI assistants handle this well — constrained pattern application
      - Easier than conditional spaghetti

      ## When to Use

      **Good fit:**
      - Polyglot projects
      - Mixed dev teams (Linux/macOS/WSL)
      - CI/CD requiring consistency

      **Skip it:**
      - Simple projects without containerization needs
      - Uniform environments with same toolchain
      - Single-target deployments
    content_hash: sha256:d9e29cb40189451f1a64658496c372a6bb035858dd77d3f33a56c05d72d80d1c
    distilled: |-
      # Container Overlay Pattern

      Same command interface, different execution context. Eliminate build file conditionals while supporting host and containerized builds.

      ## Problem

      Containerized builds for consistency, but common approaches have friction:
      - **Dual Makefiles**: `make -f Makefile.docker build` — easy to forget
      - **Conditional detection**: `ifeq`/`else`/`endif` everywhere — cluttered
      - **Different commands**: `make build` vs `make container-build` — parallel names, duplicate docs

      ## Solution

      Mount different build file over host's file inside container.

      ```
      project/
      ├── Makefile              # Host: delegates to container
      └── Makefile.container    # Container: runs directly
      ```

      **Host Makefile** (orchestration):
      ```make
      DOCKER_RUN := docker run --rm \
          -v ./:/workspace \
          -v ./Makefile.container:/workspace/Makefile:ro \
          -w /workspace myimage

      build:
          $(DOCKER_RUN) make build
      ```

      **Container Makefile** (execution):
      ```make
      build:
          cargo build
      ```

      Key: `-v ./Makefile.container:/workspace/Makefile:ro` — file swap *is* detection mechanism.

      ## Separation of Concerns

      - **Host file**: container image, volumes, network, env vars
      - **Container file**: compilation, testing, linting, build logic

      ## With just

      ```just
      # justfile (host)
      _run +ARGS:
          podman run -v ./justfile.container:/workspace/justfile:ro ... just {{ARGS}}

      build:
          just _run build
      ```

      Advantages: no `.PHONY`, natural shell variables, recipe arguments, better errors.

      ## Edge Cases

      **Devcontainer**: Set `DEVCONTAINER` env var, make `DOCKER_RUN` empty.

      **Podman**: Swap `docker` for `podman`, add `:Z` for SELinux.

      ## Tradeoffs

      Duplicated: target names, two files. But: mechanical duplication, not logical mixing; AI handles well.

      ## When to Use

      **Good**: polyglot projects, mixed dev teams, CI consistency.
      **Skip**: simple projects, uniform environments, single-target deployments.
    distilled_by: claude-code

  testcontainers:
    tags:
      - tooling
      - testing
      - containers
      - integration-testing
      - bit
    content: |-
      # Testcontainers

      Lib for throwaway containers in tests—real deps (DBs, brokers) vs mocks.

      ## Test Types (by what, not how)

      | Type | Tests | Location |
      |------|-------|----------|
      | Unit | Pure logic | Adjacent `.test` |
      | BIT | Single impl vs interface | Adjacent `.test` (w/ testcontainers) |
      | Integration | Multi-component | `tests/` |
      | E2E | Full system | Separate project |

      **BITs w/ testcontainers = unit-adjacent.** Test one thing, fast, colocated.

      ## Mock vs Real

      **Testcontainers:** DBs, brokers, controlled infra
      **Mocks:** External APIs, failure injection

      If cheap to test real, do it.

      ## Core

      - Real services as Docker/Podman containers
      - Auto cleanup, randomized ports, cross-platform

      ## Go Quick Start

      ```go
      container, err := testcontainers.GenericContainer(ctx, testcontainers.GenericContainerRequest{
          ContainerRequest: testcontainers.ContainerRequest{
              Image:        "postgres:16-alpine",
              ExposedPorts: []string{"5432/tcp"},
              Env:          map[string]string{"POSTGRES_PASSWORD": "test", "POSTGRES_DB": "testdb"},
              WaitingFor:   wait.ForLog("database system is ready").WithOccurrence(2),
          },
          Started: true,
      })
      defer container.Terminate(ctx)
      host, _ := container.Host(ctx)
      port, _ := container.MappedPort(ctx, "5432")
      ```

      ## Wait Strategies

      ```go
      wait.ForLog("Ready")
      wait.ForHTTP("/health").WithPort("8080/tcp")
      wait.ForListeningPort("5432/tcp")
      wait.ForSQL("5432/tcp", "postgres", connStrFunc)
      wait.ForAll(wait.ForLog("Started"), wait.ForHTTP("/ready"))
      ```

      ## Lifecycle

      `Start(ctx)`, `Stop(ctx, nil)`, `Terminate(ctx)`

      ## Modules (Pre-configured)

      ```go
      container, _ := postgres.Run(ctx, "postgres:16-alpine",
          postgres.WithDatabase("testdb"), postgres.WithUsername("user"), postgres.WithPassword("pass"))
      connStr, _ := container.ConnectionString(ctx, "sslmode=disable")
      ```

      Modules: PostgreSQL, MySQL, Redis, MongoDB, Kafka, RabbitMQ, Elasticsearch, LocalStack, etc.

      ## Networks

      ```go
      req := testcontainers.ContainerRequest{
          Networks:       []string{"test-network"},
          NetworkAliases: map[string][]string{"test-network": {"myservice"}},
      }
      ```

      ## Files/Config

      ```go
      container.CopyFileToContainer(ctx, "local.conf", "/etc/app/config.conf", 0644)
      // Mounts: testcontainers.BindMount("/host", "/container")
      // Files: ContainerFile{HostFilePath: "init.sql", ContainerFilePath: "/docker-entrypoint-initdb.d/init.sql"}
      ```

      ## Exec/Logs

      ```go
      exitCode, reader, _ := container.Exec(ctx, []string{"psql", "-U", "postgres", "-c", "SELECT 1"})
      logs, _ := container.Logs(ctx)
      ```

      ## Reusable Containers

      ```go
      req := testcontainers.ContainerRequest{Name: "reusable-postgres"}
      req = req.WithReuse(ctx)
      ```

      ## Test Categorization

      ```go
      //go:build testcontainers
      func TestPostgresStorage(t *testing.T) { /* ... */ }
      ```

      ```rust
      #[cfg_attr(not(feature = "testcontainers"), ignore)]
      fn test_postgres_storage() { /* ... */ }
      ```

      ## Best Practices

      1. Use modules when available
      2. Always `defer Terminate()`
      3. Proper wait strategies
      4. Never hardcode ports—use `MappedPort()`
      5. Tag images explicitly, prefer alpine
      6. Parallel tests OK—each gets own container
      7. Colocate BITs next to impl

      ## Troubleshooting

      - Slow: lighter images, check wait strategy
      - Port conflicts: use `MappedPort()`
      - Not ready: add wait strategy
      - Cleanup: always `defer Terminate()`
      - CI: verify Docker access, resource limits
    content_hash: sha256:2648523e269722d2317addca1786079a5dfcc499a45f1fb83e17d652d9502040
    distilled_by: claude-code
