version: 1.0.0
tags:
    - typescript
    - language
    - development
author: ctxloom
description: TypeScript development practices, strict mode configuration, and best practices
fragments:
    typescript:
        tags:
            - typescript
            - language
        content: |-
            # TypeScript Dev

            ## Config
            - tsconfig: strict mode required (strict, noImplicitAny, strictNullChecks, noUncheckedIndexedAccess)
            - Tools: npm/pnpm/yarn, vitest/jest, ESLint+Prettier, tsc/esbuild/swc

            ## Naming
            - PascalCase: classes/interfaces/types/enums
            - camelCase: functions/variables/params
            - kebab-case: files (`user-service.ts`)
            - Tests: `.test.ts` co-located
            - CONSTANT_CASE: true globals only
            - Abbrevs as words: `loadHttpUrl`

            ## Type Safety
            - Never: `any`, `@ts-ignore`, type assertions (`as`), non-null assertions (`!`)
            - Use `unknown` for indeterminate types
            - Explicit return types for public APIs

            ## Imports/Exports
            - ES modules only, named exports only (no default)
            - `import type` for type-only
            - Include `.js` extension for ESM

            ## Variables
            - Never `var`; prefer `const`, use `let` if reassignment needed

            ## Interfaces vs Types
            - `interface` for objects; `type` for unions/intersections/mapped/primitives

            ## Classes
            - `readonly` for immutable props
            - Parameter properties: `constructor(private readonly x: X)`
            - Use TS visibility (`private`/`protected`), not `#`

            ## Functions
            - Declarations for named fns, arrows for callbacks/this-binding

            ## Errors
            - Throw `Error` instances only
            - No empty catch blocks

            ## Control Flow
            - Always use braces
            - `===`/`!==` only (except `== null`)

            ## Disallowed
            var, any, @ts-ignore, @ts-nocheck, default exports, namespace, eval(), prototype modification, wrapper objects
        content_hash: sha256:ca096ef678e217e8c207276af34ee481af7c8d891199a60c00187b9b40eaec8c
        distilled: |-
            # TypeScript Dev

            ## Config
            - tsconfig: strict mode (strict, noImplicitAny, strictNullChecks, noUncheckedIndexedAccess)
            - Tools: npm/pnpm/yarn, vitest/jest, ESLint+Prettier, tsc/esbuild/swc

            ## Naming
            - PascalCase: classes/interfaces/types/enums
            - camelCase: functions/variables/params
            - kebab-case: files (`user-service.ts`)
            - Tests: `.test.ts` co-located
            - Abbrevs as words: `loadHttpUrl`

            ## Type Safety
            - Never: `any`, `@ts-ignore`, assertions (`as`), non-null (`!`)
            - Use `unknown` for indeterminate; explicit return types for public APIs

            ## Imports/Exports
            - ES modules, named exports only
            - `import type` for types; include `.js` extension

            ## Variables
            - `const` preferred, `let` if needed, never `var`

            ## Interfaces vs Types
            - `interface` for objects; `type` for unions/mapped/primitives

            ## Classes
            - `readonly` for immutable; parameter properties in constructor
            - TS visibility (`private`/`protected`), not `#`

            ## Functions
            - Declarations for named, arrows for callbacks

            ## Errors
            - Throw `Error` only; no empty catch

            ## Control Flow
            - Always braces; `===`/`!==` (except `== null`)

            ## Disallowed
            var, any, @ts-ignore, @ts-nocheck, default exports, namespace, eval(), prototype modification, wrapper objects
        distilled_by: claude-3-opus
